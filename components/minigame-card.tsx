import { Card, CardContent } from "@mui/material";

export default function MiniGameCard(){

    return <Card>
        <CardContent>
            - have you added an item this hour: 
            - items added last 24 hours:
            - notification 
        </CardContent>
    </Card>

}